#declarar variables
acumulado=int (0)
número=str("")
#formamos un ciclo infinito hasta que esté se rompa 
while true:
    número=input("dame un número entero: ")
    if número=="" "":
      #si no ponen número 
      print("vacio"
     break
    else:
     acumulado+int(número)
     salida="monto acumulado: {}"
     print(salida.format(acumulado))
      #si se da el valor
      #se realiza el cálculo usando suma